// test code for, well, testing

include boxes;

foo: int; // main.foo (int)
bar: bool; // main.bar (int)
box: Box; // main.box (box.Box)

main: void () {
    test: int = 1;
}

class BetterBox: Box {

    my_field: char = 'a';
    other_box: Box;

    wrap: int (hello: float) {
        foo: int = 10;
        foo = foo + 10;
        return foo;
    }
}
