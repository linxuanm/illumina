#include "primitives.h"

/*TypeObject IntType = {
    sizeof(int64_t)
};

TypeObject FloatType = {
    sizeof(double)
};

TypeObject CharType = {
    sizeof(int32_t)
};

TypeObject ByteType = {
    sizeof(uint8_t)
};*/